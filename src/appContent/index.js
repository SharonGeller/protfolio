import React from "react";
import Frame from "./Frame";
import Routes from "../routes";

const AppContent = () => {
  return (
    <Frame>
      <Routes />
    </Frame>
  );
};

export default AppContent;
